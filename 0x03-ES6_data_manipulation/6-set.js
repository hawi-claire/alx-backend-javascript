/**
 * Function that returns a Set from an array
 * @param {Array} array - The input array
 * @return {Set} A Set containing the array elements
 */
const setFromArray = (array) => {
  return new Set(array);
};

export default setFromArray;
